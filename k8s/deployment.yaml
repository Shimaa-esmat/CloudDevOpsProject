apiVersion: apps/v1
kind: Deployment
metadata:
  name: gp-deployment
  namespace: ivolve
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flask
  template:
    metadata:
      labels:
        app: flask
    spec:
      containers:
      - name: flask
        image: gp-ivolve
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 5000
